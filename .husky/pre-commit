#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "Running lint-staged on staged files..."
# Use npx so it works without adding extra npm script; lint-staged will run eslint --fix on staged files
npx --no-install lint-staged
